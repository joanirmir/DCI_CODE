# ThinkPython - Strings


#i = 1
#fruit = 'banana'

#print(fruit[i+1])

"""# len is a built-in function that returns the number of characters in a string:
fruit = 'banana'
print(len(fruit))

length = len(fruit)
last = fruit[length-1]
print(last)

# Traversal with a for loop

index = 0
while index < len(fruit):
    letter = fruit[index]
    print(letter)
    index = index + 1

# Write a function that takes a string as an argument and displays the letters backward, one per line:

def letters_backward(string):
    index = (len(string)-1)
    while index >= 0:
        letter = string[(index)]
        print(letter)
        index = index - 1

print(letters_backward('banana'))"""
